# 项目交付规则

- 每次完成本项目开发后，必须在用户桌面保留并更新同一个入口：`风从旧世来（最新版）.lnk`。
- 入口指向本次已验证的最新可运行版本，不按版本号累积快捷方式。不因目录名称含 v010 就误判当前工程为旧版。
- Windows 开发环境可运行 `design/update-desktop-shortcut.ps1 -SdkExecutable <renpy.exe绝对路径>` 更新入口。它直接启动当前工程，不能代替独立分发包。
- 完工时检查快捷方式目标、项目参数和工作目录有效；若切换实验分支或工程路径，交付前重新核对。不得擅自切分支、拉取代码或覆盖未提交修改来维持入口。
- 清理旧版前确认对象；优先移入回收站。保护当前工程、真实玩家存档、原始美术、签名密钥、SDK 和保留的旧菜单 Git 分支。原型源码不因年代久远就自动删除。
- 不执行 SHA、全文件树哈希或像素比较验收。
