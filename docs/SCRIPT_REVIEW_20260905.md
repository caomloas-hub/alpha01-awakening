# 新 Skill 实战复查：序章与城镇 Pt.1

范围：`game/script.rpy`、`game/routes/town/town_route.rpy`，并审读 `game/routes/route_select.rpy` 的衔接。不是全文重写；保留事件、菜单、标签、条件、数值和持久化赋值。

## 已修正

| 问题 | 修改 |
| --- | --- |
| 原型体房间先说灯全灭，随后又关报警红灯 | 改成生命读数归零、报警仍在闪 |
| 物资先被断言可用，随后说药不可信；水无来源 | 只确认带走封装绷带，不使用失效日期不明的药，不凭空拿水 |
| 没选搜物资也反复使用应急袋 | 公共走廊补一个取得空袋的动作；可选分支只影响内容物 |
| 主角推测商队分工却写成事实 | 保留“看站位，像是”的推断范围 |
| 没检查车轴的路径也说裂声“比预想更脆” | 改为共同可听见的断裂声 |
| 二十五枚小铜分“一半”，且前半笔交付没写 | 明确十二加十三，补预付动作；最终仍扣两枚、余额二十三 |
| 岚先说口音像北方，夜里又说不像 | 编造来处时承认口音生，不先作相反断言 |
| 工作衣已经摊平，剪刀却又靠近身体尾根 | 比量标记、脱下、摊平剪缝、穿好的顺序 |
| 地图说还有半日，重新上路就已将近日落 | 道路段加经过一段路程后的时间表达，保留半日路程信息 |
| 岚引用主角只在旁白想过的“七成” | 改为他能观察到的理解迟疑 |
| 闻策没被告知识旧字却直接警告解碑 | 保留原条件与记号，仅询问可核实的工作事实 |
| 不选加固任务也被称为安装过木撑 | 证言改为所有路线实际参与过的稳车救援 |
| 声音“涌进视野”及城市设施重复解释 | 分开听觉视觉，以一次让路动作接住观察 |
| 未带走核心的路线，夜间行李里也有核心 | 去掉无条件核心清点，不借其他周目 persistent 补当前物品 |
| 岚默认主角既会修车又认得桥中文字 | 改用共同救援事实与当日货币交流，不跨可选分支引用 |
| 夜谈重复“先别谢”及同样的低灯动作 | 保留一处玩笑，另一处落实明日搬货，消除重复灯芯动作 |
| 临别仍像格言 | 改成叫醒与钱物收好的具体叮嘱，对答同步修顺 |

其中“公共取得空应急袋”“十二枚预付的动作”是为既有道具/约定补上的小型连续性新增，明确记录，不冒称原文已有。不增加新人物、能力、奖励或资产需求。第一夜保留了对设施寂静与人声的对照；它有剧情经历支撑，不因带有比喻或总结性质就一律删除。

当前 `town_money` 仍在原结算点写入 23，前段没有交易界面；未来若增加路上消费，必须把预付也建模到经济状态，不能只沿用目前叙述。

## 验证证据

2026-09-05，Ren'Py 8.5.3，Windows：

- lint：通过，无错误项。
- compile：通过，退出码 0。
- Skill 官方结构验证：通过；验证环境使用临时 uv 依赖及 UTF-8，不修改用户 Python 安装。
- 逐用例、独立保存目录运行：`argue_technology_route`、`silent_observer_route`、`prototype_memorial_route`、`town_minimal_road_route`、`town_ordered_axle_route`、`town_learned_tags_route`。六条均得到测试报告 `Status: PASSED`，各运行一条、跳过其余九条。现有 `$ assert` 检查随各用例执行，不把报告中专用 Assert 节点计数 0 解释成“零状态检查”。
- 最终这组六条使用 `RENPY_RENDERER=angle2`。第一轮默认 gl2 的抗辩用例发生过原生退出 `-1073741819`；没有据此认定是文本问题，也不宣称其根因已修复。
- 测试报告结束后窗口进程继续驻留；检查 SDK 发现 `EndPhase.enter()` 负责结束报告与鼠标状态，并没有退出进程调用。本轮包装器等候 45 秒后清理测试进程；因此清理后的退出码不作为剧情是否通过的依据。没有关闭用户的其他程序。
- 本地详细日志保留在工作区 `work/skill-review-angle-20260905-152135/`，不将机器路径和运行产物提交为游戏内容。

本轮未修改测试断言以迎合结果。没有执行全部选项的笛卡尔积、真实 120 秒触发测试、存读档往返、像素对比、音频验收或 Android 构建。三条城镇用例是从初始化夹具进入第一章，不冒充从序章完整走到城镇的端到端测试。

## 后续边界

此次复查发现的明确文本问题已修补；这不是“所有内容绝无问题”的保证。文字效果留给作者逐段定调。默认 gl2 的原生退出值得在下一次 Windows 运行验证中留意，但本轮不擅自改用户图形设置或 SDK。

离线核心如果在村庄 Pt.1 产生实际作用，需要独立的本周目持有状态及旧存档兼容设计。村庄目前仍是预告入口，本轮只提交 [村庄 Pt.1 计划](VILLAGE_ROUTE_PT1_PLAN.md)，不扩写运行时剧情。
